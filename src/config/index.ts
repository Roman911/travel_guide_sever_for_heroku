import { config } from 'dotenv';
const { parsed } = config();

const {
  NODE_ENV,
  MONGO_DB_KEY,
  JWT_ACCESS_SECRET,
  JWT_REFRESH_SECRET,
  PORT,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_PASSWORD,
  API_URL,
  CLIENT_URL,
  USER,
  CLIENT_ID,
  CLIENT_SECRET,
  REFRESH_TOKEN,
  GOOGLE_DEVELOPERS,
} = parsed;

export {
  NODE_ENV,
  MONGO_DB_KEY,
  JWT_ACCESS_SECRET,
  JWT_REFRESH_SECRET,
  PORT,
  SMTP_HOST,
  SMTP_PORT,
  SMTP_PASSWORD,
  API_URL,
  CLIENT_URL,
  USER,
  CLIENT_ID,
  CLIENT_SECRET,
  REFRESH_TOKEN,
  GOOGLE_DEVELOPERS,
};
